// Contains code for the actions for reducers

// Toggles the state of the sidebar i.e. collapses or expands it (the sidebar is expanded by default)
export const toggleMenuState = () => {
    return {
        type: "COLLAPSE"
    }
}
